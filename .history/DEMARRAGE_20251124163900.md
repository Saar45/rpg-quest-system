# RPG Quest System - Guide de Démarrage

## 🚀 Comment démarrer le projet

### Backend (API)

1. **Naviguer vers le dossier backend :**
   ```bash
   cd backend
   ```

2. **Installer les dépendances :**
   ```bash
   npm install
   ```

3. **Vérifier le fichier .env :**
   Assurez-vous que le fichier `.env` existe avec les variables suivantes :
   ```
   PORT=3000
   MONGO_URI=mongodb://localhost:27017/rpg_quest_system
   JWT_SECRET="votre_cle_secrete_tres_longue"
   ```

4. **Démarrer MongoDB :**
   Assurez-vous que MongoDB est en cours d'exécution sur votre machine.

5. **Démarrer le serveur :**
   ```bash
   npm run dev
   ```
   
   Le serveur sera accessible sur `http://localhost:3000`

---

### Frontend (React)

1. **Naviguer vers le dossier frontend :**
   ```bash
   cd frontend
   ```

2. **Installer les dépendances :**
   ```bash
   npm install
   ```

3. **Démarrer l'application :**
   ```bash
   npm run dev
   ```
   
   L'application sera accessible sur `http://localhost:5173` (ou un autre port affiché dans le terminal)

---

## 📋 Fonctionnalités implémentées

### ✅ Backend
- ✅ Authentification (Register/Login) avec JWT
- ✅ Hachage des mots de passe avec bcrypt
- ✅ Routes protégées avec middleware d'authentification
- ✅ Gestion du profil joueur
- ✅ Système de quêtes (accepter une quête)
- ✅ Système d'inventaire (utiliser un objet)
- ✅ Routes pour récupérer les items et quêtes

### ✅ Frontend
- ✅ Page de connexion (Login)
- ✅ Page d'inscription (Register)
- ✅ Dashboard joueur
- ✅ Gestion de l'état d'authentification avec Context API
- ✅ Stockage du JWT dans localStorage
- ✅ Routes protégées
- ✅ Service API pour communication avec le backend

---

## 🔑 API Endpoints

### Authentification (Public)
- `POST /api/auth/register` - Créer un nouveau compte
- `POST /api/auth/login` - Se connecter

### Player (Protégé 🔒)
- `GET /api/player/profile` - Récupérer le profil complet
- `POST /api/player/accept-quest/:questId` - Accepter une quête
- `POST /api/player/use-item/:itemId` - Utiliser un objet

### Items (Protégé 🔒)
- `GET /api/items` - Liste de tous les items
- `GET /api/items/:id` - Détails d'un item

### Quests (Protégé 🔒)
- `GET /api/quests` - Liste de toutes les quêtes
- `GET /api/quests/:id` - Détails d'une quête

---

## 🎮 Utilisation

1. **Démarrer le backend en premier**
2. **Démarrer le frontend ensuite**
3. **Ouvrir votre navigateur sur** `http://localhost:5173`
4. **Créer un compte** via la page d'inscription
5. **Se connecter** pour accéder au dashboard

---

## 🛠️ Technologies utilisées

### Backend
- Node.js + Express
- MongoDB + Mongoose
- bcrypt (hachage de mots de passe)
- jsonwebtoken (JWT)

### Frontend
- React 19
- React Router DOM 7
- Vite
- Context API (gestion d'état)
- CSS moderne avec gradients

---

## 📝 Notes

- Le JWT expire après 7 jours
- Les mots de passe doivent contenir au moins 6 caractères
- Toutes les routes de jeu nécessitent un token JWT valide dans le header `Authorization: Bearer <token>`
